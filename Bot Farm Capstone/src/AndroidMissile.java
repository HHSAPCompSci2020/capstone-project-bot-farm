import processing.core.PImage;

public class AndroidMissile extends Projectile {
	public AndroidMissile(PImage img, double x, double y, int w, int h, String owner, int delay) {
        super(img, x, y, w, h, owner, delay);           
}
}
