import processing.core.*;

public class PlayerProjectile extends Projectile {
	public PlayerProjectile(PImage img, int x, int y, int w, int h, String owner, double angle, int delay) {
        super(img, x, y, w, h, owner, angle, delay);           
	}
}
